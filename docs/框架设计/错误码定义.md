# 错误码定义文档

## 更新日期
2026-01-20

## 错误码范围划分

- **200001-200149**: 通用错误码
- **200150-200199**: 杂项错误码
- **200200-200299**: 战斗相关错误码
- **200300-200399**: 组队相关错误码
- **200400-200499**: 副本相关错误码

---

## 战斗错误码 (200200-200299)

| 错误码 | 常量名 | 说明 |
|--------|--------|------|
| 200201 | `ERR_UnitIsDead` | Unit 已死亡 |
| 200202 | `ERR_TargetIsDead` | 目标已死亡 |
| 200203 | `ERR_AttackCooldown` | 攻击还在冷却中 |
| 200204 | `ERR_OutOfRange` | 目标超出范围 |
| 200205 | `ERR_AlreadyInBattle` | 已在战斗中 |
| 200206 | `ERR_NotInBattle` | 不在战斗中 |
| 200207 | `ERR_CombatNotStarted` | 战斗未开始 |
| 200208 | `ERR_InvalidCombatTarget` | 无效的战斗目标 |
| 200209 | `ERR_CombatEnded` | 战斗已结束 |
| 200210 | `ERR_CannotAttackSelf` | 不能攻击自己 |
| 200211 | `ERR_InsufficientMana` | 法力值不足 |
| 200212 | `ERR_SkillCooldown` | 技能还在冷却中 |
| 200213 | `ERR_SkillNotFound` | 找不到技能定义 |
| 200214 | `ERR_InvalidSkillTarget` | 无效的技能目标 |
| 200215 | `ERR_BattleNotFound` | 战斗不存在 |
| 200216 | `ERR_BattleEnded` | 战斗已结束 |
| 200217 | `ERR_BattleNotActive` | 战斗未激活 |
| 200218 | `ERR_TeamMemberInBattle` | 队员已在战斗中 |
| 200219 | `ERR_RoomNotFound` | 房间不存在 |
| 200220 | `ERR_RoomFull` | 房间已满 |
| 200221 | `ERR_RoomNotActive` | 房间未激活 |
| 200222 | `ERR_InvalidRoomState` | 无效的房间状态 |
| 200223 | `ERR_RoomPlayerLimit` | 房间人数限制 |
| 200224 | `ERR_CreateRoomFailed` | 创建房间失败 |
| 200225 | `ERR_RoomExpired` | 房间已过期 |

---

## 组队错误码 (200300-200399)

| 错误码 | 常量名 | 说明 |
|--------|--------|------|
| 200301 | `ERR_AlreadyInTeam` | 已在队伍中 |
| 200302 | `ERR_NotInTeam` | 不在任何队伍中 |
| 200303 | `ERR_NotTeamLeader` | 不是队长 |
| 200304 | `ERR_TeamFull` | 队伍已满 |
| 200305 | `ERR_TeamNotFound` | 队伍不存在 |
| 200306 | `ERR_TeamMemberNotEnough` | 队伍人数不足 |
| 200307 | `ERR_CannotLeaveTeam` | 当前状态无法离开队伍 |
| 200308 | `ERR_TeamInBattle` | 队伍在战斗中 |
| 200309 | `ERR_TeamInDungeon` | 队伍在副本中 |
| 200310 | `ERR_InvalidTeamState` | 无效的队伍状态 |

---

## 副本错误码 (200400-200499)

| 错误码 | 常量名 | 说明 |
|--------|--------|------|
| 200401 | `ERR_DungeonNotFound` | 副本配置不存在 |
| 200402 | `ERR_DungeonAlreadyStarted` | 副本已开始 |
| 200403 | `ERR_DungeonNotStarted` | 副本未开始 |
| 200404 | `ERR_DungeonEnded` | 副本已结束 |
| 200405 | `ERR_CannotEnterDungeon` | 无法进入副本 |
| 200406 | `ERR_DungeonInstanceNotFound` | 副本实例不存在 |
| 200407 | `ERR_NotInDungeon` | 不在任何副本中 |
| 200408 | `ERR_DungeonLevelNotEnough` | 等级不足无法进入副本 |
| 200409 | `ERR_DungeonConfigInvalid` | 副本配置无效 |

---

## 使用示例

### 在消息处理器中使用

```csharp
[MessageLocationHandler(SceneType.Map)]
public class C2M_StartBattleHandler : MessageLocationHandler<Unit, C2M_StartBattle, M2C_StartBattle>
{
    protected override async ETTask Run(Unit unit, C2M_StartBattle request, M2C_StartBattle response)
    {
        Scene mapScene = unit.Scene();
        BattleRoomManagerComponent roomManager = mapScene.GetComponent<BattleRoomManagerComponent>();

        // 检查玩家是否已在战斗中
        if (roomManager.IsUnitInBattle(unit.Id))
        {
            response.Error = ErrorCode.ERR_AlreadyInBattle;
            response.Message = "已有战斗在进行中";
            return;
        }

        // 检查房间是否存在
        Room room = roomManager.GetRoomById(request.battleId);
        if (room == null)
        {
            response.Error = ErrorCode.ERR_RoomNotFound;
            response.Message = "战斗房间不存在";
            return;
        }

        // 检查房间状态
        if (room.State != RoomState.Fighting)
        {
            response.Error = ErrorCode.ERR_RoomNotActive;
            response.Message = $"战斗未进行中 (State={room.State})";
            return;
        }

        // 成功
        response.Error = ErrorCode.ERR_Success;
        response.Message = "操作成功";
    }
}
```

### 在组队战斗中使用

```csharp
[MessageLocationHandler(SceneType.Map)]
public class C2M_TeamStartBattleHandler : MessageLocationHandler<Unit, C2M_TeamStartBattle, M2C_TeamStartBattle>
{
    protected override async ETTask Run(Unit leader, C2M_TeamStartBattle request, M2C_TeamStartBattle response)
    {
        Scene mapScene = leader.Scene();
        BattleRoomManagerComponent roomManager = mapScene.GetComponent<BattleRoomManagerComponent>();

        // 检查所有队员是否都在空闲状态
        foreach (long memberId in teamMemberIds)
        {
            if (roomManager.IsUnitInBattle(memberId))
            {
                Room existingRoom = roomManager.GetRoomByUnitId(memberId);
                response.Error = ErrorCode.ERR_TeamMemberInBattle;
                response.Message = $"队员 {memberId} 已在战斗中 (RoomId={existingRoom?.Id})";
                return;
            }
        }

        // 检查队伍成员数量
        if (teamMemberIds.Count < 2)
        {
            response.Error = ErrorCode.ERR_TeamMemberNotEnough;
            response.Message = "队伍人数不足，至少需要2人";
            return;
        }

        // 创建房间...
    }
}
```

### 在退出战斗中使用

```csharp
[MessageLocationHandler(SceneType.Map)]
public class C2M_ExitBattleHandler : MessageLocationHandler<Unit, C2M_ExitBattle, M2C_ExitBattle>
{
    protected override async ETTask Run(Unit unit, C2M_ExitBattle request, M2C_ExitBattle response)
    {
        BattleRoomManagerComponent roomManager = mapScene.GetComponent<BattleRoomManagerComponent>();

        // 检查玩家是否在战斗中
        Room room = roomManager.GetRoomByUnitId(unit.Id);
        if (room == null)
        {
            response.Error = ErrorCode.ERR_NotInBattle;
            response.Message = "玩家不在战斗中";
            return;
        }

        // 检查房间状态
        if (room.State != RoomState.Fighting)
        {
            response.Error = ErrorCode.ERR_InvalidRoomState;
            response.Message = $"房间状态不正确: {room.State}";
            return;
        }

        // 退出战斗...
    }
}
```

---

## 添加新错误码的规范

### 1. 选择合适的错误码范围

根据功能模块选择对应的错误码范围：
- 战斗相关：200200-200299
- 组队相关：200300-200399
- 副本相关：200400-200499

### 2. 命名规范

```csharp
// 格式：ERR_模块_具体错误
public const int ERR_RoomNotFound = 200219;
public const int ERR_TeamMemberInBattle = 200218;
```

### 3. 添加注释

```csharp
public const int ERR_RoomNotFound = 200219;  // Room not found（房间不存在）
```

### 4. 在 ErrorCode.cs 中添加

```csharp
// Combat error codes (200200-200299)
public const int ERR_YourNewError = 200226;  // Your error description
```

### 5. 更新本文档

添加新错误码到对应的表格中，并记录使用示例。

---

## 注意事项

1. **错误码唯一性**：确保每个错误码都是唯一的
2. **向后兼容**：不要修改已有错误码的含义
3. **错误消息**：提供清晰、友好的错误消息
4. **日志记录**：重要错误应该记录到日志中
5. **国际化**：错误消息应该支持多语言（可扩展）

---

## 总结

本文档定义了 ET 战斗系统的所有错误码，包括：
- ✅ 战斗相关错误码（已更新至 200225）
- ✅ 组队相关错误码
- ✅ 副本相关错误码
- ✅ 使用示例和最佳实践

在使用错误码时，请参考本文档确保一致性和正确性。

---

**文档版本**: v1.0
**创建日期**: 2026-01-20
**作者**: Droid
